@* @page means that we not using any controler  *@
@page 
@model web_invoice_generator.Pages.Clients.IndexModel
@{

}
<br>
<h2>Klientų sąrašas</h2>
<a class="btn btn-primary btn-sm mt-2 mb-2" href="/Clients/Create">Naujas klientas</a>
<table class="table">
	<thead>
		<tr>
			<th>ID</th>
			<th>Tipas</th>
			<th>Pavadinimas</th>
			<th>Adresas</th>
			<th>Kodas</th>
			<th>PVM mok. kodas</th>
			<th>Sukurtas</th>
			<th>Veiksmas</th>
		</tr>
	</thead>

	<tbody>
		@foreach(var item in Model.listClients)
		{
			<tr>
				<td>@item.id</td>
				<td>@item.type</td>
				<td>@item.name</td>
				<td>@item.address</td>
				<td>@item.code</td>
				<td>@item.vat_code</td>
				<td>@item.created_at</td>
				<td>
					<a class="btn btn-primary btn-sm col-8 m-1" href="/Clients/Edit?id=@item.id">Redaguoti</a>
					<a class="btn btn-danger btn-sm col-8 m-1" href="/Clients/Delete?id=@item.id">Ištrinti</a>
				</td>
			</tr>
		}

	</tbody>
</table>